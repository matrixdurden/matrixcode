# MatrixCode

MatrixCode is a clean-room Rust terminal coding agent focused on minimal resource use, low latency, and a small maintainable architecture.

The repository starts from a clean implementation. JCode and OpenCode may be studied as references, but they are not source-of-truth codebases for MatrixCode.
